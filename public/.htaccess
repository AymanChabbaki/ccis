<IfModule mod_rewrite.c>
  RewriteEngine On
  # If the app is in a subfolder, you might need to uncomment and set the RewriteBase
  # RewriteBase /icisct/
  
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . index.html [L]
</IfModule>
